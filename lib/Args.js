/**
 * Module depedencies.
 */

var toArray = require('to-array')

/**
 * Exposes `Args`.
 */

module.exports = Args

/**
 * Args constructor.
 * @constructor
 * @param {Array} args
 */

function Args (args) {
  this.args = args
}
